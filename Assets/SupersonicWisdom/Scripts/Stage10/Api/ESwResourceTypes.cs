#if SW_STAGE_STAGE10_OR_ABOVE
namespace SupersonicWisdomSDK
{
    // This enum is being edited each time the unity analytics events window has changed - based on the configuration.
    public enum ESwResourceTypes
    {
        
    }
}
#endif